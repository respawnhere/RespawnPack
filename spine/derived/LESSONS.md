> ⚙️ **DERIVED — do not hand-edit.** Regenerated by `savepoint --write` (`kernel/lib/render.js`) from the memory store, never from prose. Hand edits below the GENERATED marker are overwritten and flagged. (Seeded <date>.)

# LESSONS — the register

What this project has actually learned, in one place a person can read: the entities promoted into
`memory/graph/**` with the verification that earned each one, and a count of the leads still sitting in
`memory/candidates/` waiting for somebody to promote, reject or defer them. Both numbers are recounted
from those files on every run and checked back against them, so a stale integer here is a `FAIL`, not a
plausible-looking sentence.

The register is a **projection**. Nothing is written here by hand and nothing is decided here: a lead
becomes a lesson only through `respawnpack memory candidates promote <id> --as <type>/<slug>
--verified-by "<what proved it>"`, which is audited and refuses an unstated verification.

Write prose in the NOTE block below — that survives regeneration. Everything inside the GENERATED block
is rewritten.

<!-- RESPAWNPACK:NOTE — hand-written, preserved across regeneration -->
_(no human note)_
<!-- /RESPAWNPACK:NOTE -->

<!-- RESPAWNPACK:GENERATED — do not hand-edit below this line -->

_The first `savepoint --write` replaces this block with the register: one row per promoted entity, then
the counts of candidate and rejected leads. Until then this project has run no savepoint, which is a
different fact from having learned nothing._

<!-- /RESPAWNPACK:GENERATED -->
