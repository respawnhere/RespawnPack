> ⚙️ **Maintained alongside `/savepoint`, from git history — but NOT machine-regenerated.** `render.js` renders CONTINUITY.md and GAPS.md and not this file, so hand edits are NOT overwritten and `savepoint --verify` does not check this file against a source. (Seeded <date>.)

# Changelog

The session audit trail. From session 1 onward `/savepoint` updates this file from git. ⚠️ It is **not** machine-regenerated the way CONTINUITY.md and GAPS.md are: `kernel/lib/render.js` renders those two and not this one, so a changelog entry is authored, not derived, and `savepoint --verify` does not check it against its source; entries are condensed, commit refs are authoritative. **Removals are not re-described here** — a killed feature points to its [`DECISIONS.md`](../DECISIONS.md) entry by `D-id`.

---

## <session / date> — <one-line summary>
- <grouped feat/fix/docs entries from the git log>
