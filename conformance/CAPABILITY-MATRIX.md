# RespawnPack · v0.3 capability matrix

Generated by `node conformance/capability-matrix.mjs` from the four live profile-declaration modules, per
the multi-host in-place rollover design. `conformance/conformance.test.mjs`
re-renders this file on every run and asserts it is byte-identical to what is checked in — the same
rendered-claims-verified-against-source discipline `kernel/lib/render.js` uses for `CONTINUITY.md` and
`GAPS.md`. A cell here that does not match what the source modules produce today is a bug in this file,
not in them.

## How to read a cell

Every claim in this repository rests on `core/policy/capabilities.js`'s `declare()`: a capability may be
declared `SUPPORTED` or `SUPPORTED_WITH_LIMITATIONS` only when a well-formed activation canary recorded
its own outcome as `PASS` — a canary that ran and did not pass, or never ran at all, downgrades the claim
to `CANNOT_DETERMINE`, naming the real verdict rather than staying silent about it. **Declared** is what a
profile is built to reach (its target, from the v0.3 design). **Observed** is what stands today, after that
downgrade rule runs against whatever canary evidence actually exists. A cell showing one value (for example
`SUPPORTED`) means declared and observed agree — either because the target needs no canary
(`NOT_SUPPORTED`, an honest claim about a missing mechanism, not an unproven one), or because a real
passing canary backs it. A cell showing `CANNOT_DETERMINE` _(declared `SUPPORTED_WITH_LIMITATIONS`)_ means
the profile is built to reach that target and nothing today proves it — see "Canary basis per profile"
below for exactly what is missing and why. Installing files is never treated as evidence; only a fired,
passing canary is.

## The matrix

| Capability | Claude Code — interactive hooks | Claude Code — managed SDK supervisor | Codex — interactive hooks | Codex — managed app-server supervisor |
|---|---|---|---|---|
| `probe` — prove the configured integration is active | `CANNOT_DETERMINE` _(declared `SUPPORTED_WITH_LIMITATIONS`)_ | `CANNOT_DETERMINE` _(declared `SUPPORTED`)_ | `CANNOT_DETERMINE` _(declared `SUPPORTED_WITH_LIMITATIONS`)_ | `SUPPORTED_WITH_LIMITATIONS` |
| `measureContext` — return usage, capacity, source, and confidence | `CANNOT_DETERMINE` _(declared `SUPPORTED_WITH_LIMITATIONS`)_ | `CANNOT_DETERMINE` _(declared `SUPPORTED_WITH_LIMITATIONS`)_ | `NOT_SUPPORTED` | `SUPPORTED_WITH_LIMITATIONS` |
| `settleOrStop` — reach a safe boundary without duplicating work | `CANNOT_DETERMINE` _(declared `SUPPORTED`)_ | `CANNOT_DETERMINE` _(declared `SUPPORTED_WITH_LIMITATIONS`)_ | `CANNOT_DETERMINE` _(declared `SUPPORTED`)_ | `SUPPORTED` |
| `requestCompact` — ask the host to compact through a documented API | `NOT_SUPPORTED` | `CANNOT_DETERMINE` _(declared `SUPPORTED`)_ | `NOT_SUPPORTED` | `SUPPORTED` |
| `observeCompact` — prove completion or return a typed failure | `CANNOT_DETERMINE` _(declared `SUPPORTED`)_ | `CANNOT_DETERMINE` _(declared `SUPPORTED`)_ | `CANNOT_DETERMINE` _(declared `SUPPORTED_WITH_LIMITATIONS`)_ | `SUPPORTED_WITH_LIMITATIONS` |
| `injectHandoff` — add verified continuation context | `CANNOT_DETERMINE` _(declared `SUPPORTED`)_ | `CANNOT_DETERMINE` _(declared `SUPPORTED`)_ | `CANNOT_DETERMINE` _(declared `SUPPORTED_WITH_LIMITATIONS`)_ | `SUPPORTED` |
| `resume` — continue the same conversation identity | `CANNOT_DETERMINE` _(declared `SUPPORTED`)_ | `CANNOT_DETERMINE` _(declared `SUPPORTED`)_ | `CANNOT_DETERMINE` _(declared `SUPPORTED_WITH_LIMITATIONS`)_ | `SUPPORTED_WITH_LIMITATIONS` |

## Rollover-capable today

A profile is rollover-capable only when every capability the in-place rollover needs
(`core/policy/capabilities.js`'s `ROLLOVER_REQUIRED`, all seven above) is at least
`SUPPORTED_WITH_LIMITATIONS` — a profile that cannot observe completion has not "mostly" rolled over, it
has not proven one happened.

| Profile | Kind | Rollover-capable today | Unmet (if any) |
|---|---|---|---|
| Claude Code — interactive hooks (`claude-interactive-hooks`) | interactive, manual `/compact` | no | `probe`, `measureContext`, `settleOrStop`, `requestCompact`, `observeCompact`, `injectHandoff`, `resume` |
| Claude Code — managed SDK supervisor (`claude-code/sdk-supervisor`) | managed, automatic | no | `probe`, `measureContext`, `settleOrStop`, `requestCompact`, `observeCompact`, `injectHandoff`, `resume` |
| Codex — interactive hooks (`codex-interactive-hooks`) | interactive, manual `/compact` | no | `probe`, `measureContext`, `settleOrStop`, `requestCompact`, `observeCompact`, `injectHandoff`, `resume` |
| Codex — managed app-server supervisor (`codex/app-server`) | managed, automatic | yes | — |

## Canary basis per profile

- **Claude Code — interactive hooks** (`claude-interactive-hooks`): No activation canary is recorded in this repository. `probe.js` looks for a live SessionStart baseline and a context-monitor latch file written for a real conversation; this repository is the pack’s own source tree, not an installed target with one running.
- **Claude Code — managed SDK supervisor** (`claude-code/sdk-supervisor`): A live activation canary ran on this machine and answered CANNOT_DETERMINE (exit 2): the PATH `claude` binary reported "Not logged in" (2.1.205), and the SDK’s own bundled binary separately reported an expired OAuth session. Both are environmental — an unauthenticated context, not a defect in this profile. See adapters/claude-code/sdk-supervisor/README.md.
- **Codex — interactive hooks** (`codex-interactive-hooks`): No activation canary is recorded in this repository. Codex hooks are gated on BOTH `features.hooks` and a per-hook interactive `/hooks` trust decision inside a live Codex session; neither has been established here.
- **Codex — managed app-server supervisor** (`codex/app-server`): A live activation canary ran on this machine and PASSED: three consecutive in-place rollovers on one real Codex thread (codex-cli 0.146.0, 2026-08-06T17:54:36.632Z). The event log and the structured report are checked in verbatim and digest-fenced at adapters/codex/app-server/fixtures/captured/05-live-canary-events.jsonl and 06-live-canary-report.json — both flagged `containsEnvironmentData` in the fixture MANIFEST (this machine’s temp paths, the host’s codexHome, the userAgent, and unsolicited account/rate-limit rows are recorded unredacted, because a redacted evidence log is not evidence). Owner review before any publication.

## Pi support

Pi is not one of the profiles in this matrix. RespawnPack carries no Pi adapter; Pi support ships as the separate `respawn-pi` package, which builds on this pack’s host-neutral `core/` contract without forking it and declares its own capabilities there.
